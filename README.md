# OCaml Dataflow

## Prerequisites

Install OCaml and Dune.

## Building

Run `dune build` at the root level.

Run `dune exec bin/main.exe` or the file you want to execute.

Run `dune clean` to clean the build artifacts.

## Formatting

Run `dune fmt`.
